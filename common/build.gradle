apply plugin: 'com.android.library'

android {
    compileSdkVersion 19
    buildToolsVersion "20.0.0"

    defaultConfig {
        applicationId "pl.tajchert.swearcommon"
        minSdkVersion 18
        targetSdkVersion 19
        versionCode 3
        versionName "0.3"
    }
    buildTypes {
        release {
            runProguard false
        }
    }
}
