apply plugin: 'com.android.library'

android {
    compileSdkVersion 19
    buildToolsVersion "21.1.2"

    defaultConfig {
        minSdkVersion 18
        targetSdkVersion 19
        versionCode 9
        versionName "0.9"
    }
    buildTypes {
        release {
            minifyEnabled false
        }
    }
}
